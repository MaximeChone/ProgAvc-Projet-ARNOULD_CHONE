#include <stdio.h>

enum case_type {chemin_horizontal,chemin_vertical,chemin_HG,chemin_BG,chemin_HD,chemin_BD,herbe,montagne,deb,fin};
typedef enum case_type case_type;
struct case_map
{
  case_type terrain; //le type de terrain
  SDL_Surface *image; //image du terrain
  int occup; //si la case est occupé
};
