SRC=TowerDefense.c
BIN=TowerDefense

$(BIN): $(SRC)
	gcc $(SRC) -Wall -g -std=c99 `sdl-config --cflags --libs`  -lm -o $(BIN)

fonction.o: fonction.c 
	gcc -o fonction.o -c fonction.c -g -W -Wall -std=c99 `sdl-config --cflags --libs`

clean:
	rm -f $(BIN) *.o
